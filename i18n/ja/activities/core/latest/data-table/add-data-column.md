---
id: add-data-column
title: "データ列の追加"
sidebar_label: "データ列の追加"
sidebar_position: 1
description: "データ列アクティビティのドキュメントを追加します。"
displayed_sidebar: activitiesSidebar
---
# データ列の追加

RCA.Activities.Core.AddDataColumn

## **説明**

このアクティビティを使用すると、指定した DataTable に DataColumn を追加できます。

![add data column.jpg](/static/img/b21ad7_add-data-column.jpg)

（\*必須）

## **プロパティ**

**入力**

* **Column (DataColumn)**\* - DataTable の列コレクションに追加される DataColumn オブジェクト。
* **列名 (文字列)**\* - 指定した DataTable オブジェクトに追加する新しい列の名前。文字列のみがサポートされます。
* **データ テーブル (DataTable)**\* - 列を追加する DataTable オブジェクト。このフィールドは DataTable オブジェクトのみをサポートします。

**その他**

* **公開 (チェックボックス)** - 公開したい場合にチェックを入れます。使用する前に、データのセキュリティ要件を必ず考慮してください。
* **表示名 (文字列)** - このアクティビティの名前。アクティビティの名前を編集して、コードをより適切に整理および構造化できます。
**例**: データ列の追加
* **型引数 (型)** - ジェネリック型のインスタンス化に使用される参照型

**オプション**

* **DBNull を許可 (チェックボックス)** - テーブルに属する行の新しい列で新しい値を許可するかどうかを指定します。
* **増加を許可** - 新しい行が追加されたときに列の値が自動的に増加するかどうかを指定します。
* **デフォルト値 (オブジェクト)** - 新しい行を作成するときの新しい列のデフォルト値を指定します。値は、TypeArgument プロパティで指定された型である必要があります。
* **最大長 (整数)** - 新しい列の各行の最大長を指定します。 Int32 変数のみをサポートします。
* **一意 (チェックボックス)** - 新しい列の各行の値が一意である必要があることを指定します。
