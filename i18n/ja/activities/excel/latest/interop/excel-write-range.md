---
id: excel-write-range
title: "Excel書き込み範囲"
sidebar_label: "Excel書き込み範囲"
sidebar_position: 24
description: "Excel の範囲書き込みアクティビティのドキュメント。"
displayed_sidebar: activitiesSidebar
---
# Excel書き込み範囲

RCA.Activities.Excel.ExcelWriteRange

## **説明**

このアクティビティを使用すると、StartingCell フィールドに示されているセルから始まるスプレッドシートの DataTable 変数のデータを書き込むことができます。開始セルが指定されていない場合、データは A1 セルから書き込まれます。シートが存在しない場合は、Sheetname プロパティで指定された値を使用して新しいシートが作成されます。指定した範囲内のすべてのセルが上書きされます。変更はすぐに保存されます。 Excel アプリケーション スコープ アクティビティでのみ使用できます。

![image-20220505114134-2.jpeg](/static/img/46cc2c_image-20220505114134-2.jpeg)

（\*必須）

## **アクティビティ本体内**

* **シート名** - 書き込みたいシート。デフォルトでは、「Sheet1」が入力されます。文字列変数と文字列のみがサポートされます。例: 「シート 1」
* **開始セル** - データの書き込みを開始するセル。文字列変数と文字列のみがサポートされます。
例：「A10」

* **データ テーブル** - 指定した範囲に DataTable 変数として書き込むデータ。 DataTable 変数のみがサポートされます。

## **プロパティ**

**入力**

* **データ テーブル (DataTable)**\* - 指定された範囲に DataTable 変数として書き込むデータ。 DataTable 変数のみがサポートされます。
* **シート名 (文字列)\***- 書き込みたいシート。デフォルトでは、「Sheet1」が入力されます。文字列変数と文字列のみがサポートされます。
例: 「クライアントのデータ」
* **開始セル (文字列)\***- データの書き込みを開始するセル。文字列変数と文字列のみがサポートされます。
例：「A10」

**その他**

* **公開 (チェックボックス)** - 公開する場合にチェックを入れます。使用する前に、データ セキュリティ要件を必ず考慮してください。
* **表示名 (文字列)** - このアクティビティの名前。アクティビティの名前を編集して、コードをより適切に整理および構造化できます。
例: Excel 範囲の書き込み

**オプション**

* **ヘッダーの追加 (チェックボックス)**- 選択すると、列ヘッダーも指定された範囲に書き込まれます。デフォルトでは、このチェックボックスは選択されていません。
