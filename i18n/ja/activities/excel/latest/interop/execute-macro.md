---
id: execute-macro
title: "マクロの実行"
sidebar_label: "マクロの実行"
sidebar_position: 25
description: "マクロアクティビティのドキュメントを実行します。"
displayed_sidebar: activitiesSidebar
---
# マクロの実行

RCA.Activities.Excel.ExecuteMacro

## **説明**

このアクティビティを使用すると、ワークブック内でマクロを実行できます。ワークブック ファイルはマクロが有効なワークブックである必要があります。変更はすぐに保存されます。 Excel アプリケーション スコープ アクティビティでのみ使用できます。

![image-20220505134614-1.jpeg](/static/img/460a39_image-20220505134614-1.jpeg)

（\*必須）

## **プロパティ**

**共通**

* **ContinueOnError (ブール値)** - アクティビティがエラーをスローした場合でも自動化を続行するかどうかを指定します。ブール値 (True、False) のみがサポートされます。
・True - エラーが発生してもプロセスの実行は続行されます。
・False (デフォルト) - プロセスの実行継続を停止します。

**入力**

* **マクロ名 (文字列)** - 実行されるマクロの名前。文字列変数と文字列のみがサポートされます。
例：「マルコ1号」
* **マクロ パラメーター (IEnumberable< Object >)** - マクロの実行時にパラメーターを渡すことができます。このフィールドは IEnumerable 変数のみをサポートします。

**その他**

* **公開 (チェックボックス)** - 公開する場合にチェックを入れます。使用する前に、データ セキュリティ要件を必ず考慮してください。
* **表示名 (文字列)** - このアクティビティの名前。アクティビティの名前を編集して、コードをより適切に整理および構造化できます。
例：「マルコ処刑」

**出力**

* **マクロ出力 (オブジェクト)** - マクロの実行によって返される値 (存在する場合)。この出力はオブジェクト変数として取得されます。
