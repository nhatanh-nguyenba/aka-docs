---
id: take-screenshot
title: "スクリーンショットを撮る (注意)"
sidebar_label: "スクリーンショットを撮る (注意)"
sidebar_position: 22
description: "スクリーンショット (NB) アクティビティのドキュメントを取得します。"
displayed_sidebar: activitiesSidebar
---
# スクリーンショットを撮る (注意)

RCA.Activities.NativeBrowser.TakeScreenshot

## **説明**

スクリーンショットの取得アクティビティは、ブラウザー内の UI 要素のスクリーンショットをキャプチャします。

![image-20220505151520-1.png](/static/img/image-20220505151520-1.png)

\* は必須フィールドを示します。

## **アクティビティの本文内**

* **ターゲット要素を選択**\*: ブラウザー上の UI 要素を選択します。このアクションにより、要素を識別するための **Selector** 文字列が生成されます。

## **プロパティ**

**共通**

* **エラー時に続行 (ブール値)**: エラーが発生したときにオートメーションを続行するかどうかを指定します。
True: アクティビティ内でエラーが発生しても実行を継続します。
False (デフォルト): エラーが発生したときにプロセスを停止します。
* **Delay After (Int32)**: アクティビティ実行後の遅延時間 (ミリ秒単位)。デフォルトは`300`です。
例:`300`
* **Delay Before (Int32)**: アクティビティが開始されるまでの遅延時間 (ミリ秒単位)。デフォルトは`300`です。
例:`300`
* **タイムアウト MS (Int32)**: アクティビティの実行を待機する最大時間 (ミリ秒単位)。タイムアウトが経過すると、実行は停止します。デフォルトは`30000`です。
例:`30000`

**入力**

* **セレクター (文字列)**\*: ターゲット UI 要素を識別するために使用される XML 文字列。これには、要素の属性とその階層が含まれます。
* **表示を待機 (チェックボックス)**: 選択すると、ターゲット要素が表示されるまで待機してから実行します。このオプションはデフォルトで選択されています。

**その他**

* **表示名 (文字列)**: このアクティビティの名前。これを編集して、ワークフローをより適切に整理および構造化できます。
* **パブリック (チェックボックス)**: 選択すると、アクティビティ データが記録されます。このプロパティを使用する前に、データ セキュリティ要件を考慮してください。デフォルトではチェックが入っていません。

**出力**

* **出力スクリーンショット (画像)**: キャプチャされたスクリーンショット。画像オブジェクトとして返されます。