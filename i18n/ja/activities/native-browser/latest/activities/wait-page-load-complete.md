---
id: wait-page-load-complete
title: "ページのロードが完了するまで待機します (注意)"
sidebar_label: "ページのロードが完了するまで待機します (注意)"
sidebar_position: 25
description: "ページの読み込み完了を待つ (NB) アクティビティのドキュメント。"
displayed_sidebar: activitiesSidebar
---
# ページのロードが完了するまで待機します (注意)

RCA.Activities.NativeBrowser.WaitPageLoadComplete

## **説明**

「ページの読み込み完了を待つ」アクティビティを使用すると、Web ページが完全に読み込まれるまで待つことができます。

![image-20220505151720-1.png](/static/img/image-20220505151720-1.png)

\* は必須フィールドを示します。

## **プロパティ**

**共通**

* **エラー時に続行 (ブール値)**: エラーが発生したときにオートメーションを続行するかどうかを指定します。
True: アクティビティ内でエラーが発生しても実行を継続します。
False (デフォルト): エラーが発生したときにプロセスを停止します。
* **Delay After (Int32)**: アクティビティ実行後の遅延時間 (ミリ秒単位)。デフォルトは`300`です。
例:`300`
* **Delay Before (Int32)**: アクティビティが開始されるまでの遅延時間 (ミリ秒単位)。デフォルトは`300`です。
例:`300`

**その他**

* **表示名 (文字列)**: このアクティビティの名前。これを編集して、ワークフローをより適切に整理および構造化できます。
* **パブリック (チェックボックス)**: 選択すると、アクティビティ データが記録されます。このプロパティを使用する前に、データ セキュリティ要件を考慮してください。デフォルトではチェックが入っていません。

**出力**

* **待機成功 (ブール値)**: ページが完全にロードされたかどうかを示します。
True: ページは完全にロードされています。
False: ページは完全には読み込まれていません。