---
id: take-screenshot
title: "スクリーンショットを撮る"
sidebar_label: "スクリーンショットを撮る"
sidebar_position: 27
description: "スクリーンショットアクティビティのドキュメントを取得します。"
displayed_sidebar: activitiesSidebar
---
# スクリーンショットを撮る

RCA.Activities.Windows.TakeScreenshot

## **説明**

スクリーンショットの取得アクティビティを使用すると、ウィンドウのスクリーンショットを取得できます。

![image-20220505171229-1.png](/static/img/ed6028_image-20220505171229-1.png)

（\*必須）

## **アクティビティ本体内**

* **ターゲット要素を選択**\* - スクリーンショットを撮る要素を選択します。このアクティビティは、その要素の場所を指定する文字列変数 (セレクター) を生成します。

## **プロパティ**

**共通**

* **エラー時に続行 (ブール値)** - ブール変数には、True または False の 2 つの可能な値があります。
  - True: アクティビティ内でエラーが発生した場合でも、プロセスの残りの部分が実行を継続できるようにします。
  - False: プロセスの実行の継続をブロックします。
* **TimeOut MS (Int32)** - アクティビティの実行を待機する時間 (ミリ秒単位)。デフォルトの時間は 30000 ミリ秒です。
例: 30000
* **Delay After (Int32)** - アクティビティ実行後の遅延時間 (ミリ秒単位)。
例: 300
* **Delay Before (Int32)** - アクティビティが操作の実行を開始するまでの遅延時間 (ミリ秒単位)。
例: 300

**入力**

* **セレクター (文字列)\***- ウィンドウの位置を識別する文字列。

**その他**

* **公開 (チェックボックス)** - アクティビティを公開する場合にチェックを入れます。このプロパティを使用する前に、データ セキュリティ要件を必ず考慮してください。
* **表示名 (文字列)** - このアクティビティの名前。アクティビティの名前を編集して、コードをより適切に整理および構造化できます。
例: [780053] スクリーンショットを撮る

**オプション**

* **オートメーション ベース (ドロップダウン リスト)** - オートメーション ライブラリのバージョンを指定します。このオプションは、Process 引数 (UIA2 または UIA3) と併用します。
* **要素 (AutomationElement)** - セレクター文字列の代わりに、type = ‘AutomationElement’ の変数を入力として使用します。
* **From (UiElement)** - セレクター文字列で要素を検索する場合、ルートへの UiElement 変数 (入力されていない場合は、デスクトップのルートから検索します)。
* **ウィンドウを使用する (WindowApp)** - オープン ウィンドウまたはウィンドウ スコープからの出力変数を使用して、アクティビティのスコープを設定します (From と同様)。

**出力**

* **スクリーンショット (画像)** - 出力されたスクリーンショット。このプロパティは、type = “Image” の変数をサポートします。出力変数は、別のウィンドウ スコープまたは他のウィンドウ アクティビティの [ウィンドウを使用] プロパティで使用できます。出力名に空白を使用しないでください。

**待ってください**

* **無効化を待機 (チェックボックス)** - 要素が操作できなくなるまで待機します。
* **有効化を待機 (チェックボックス)** - 要素が操作できるようになるまで待機します。
* **消滅を待つ (チェックボックス)** - 要素が消滅するのを待ちます。
