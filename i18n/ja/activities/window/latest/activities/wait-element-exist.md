---
id: wait-element-exist
title: "要素が存在するのを待つ"
sidebar_label: "要素が存在するのを待つ"
sidebar_position: 21
description: "「要素の存在を待機」アクティビティのドキュメント。"
displayed_sidebar: activitiesSidebar
---
# 要素が存在するのを待つ

RCA.Activities.Windows.WaitElementExist

## **説明**

UI 要素が表示されるのを待機し、その中で複数のアクションを実行できるようにするコンテナー。

![image-20220505170814-1.png](/static/img/656128_image-20220505170814-1.png)

## **アクティビティ本体内**

* **ターゲット要素を選択** - 表示されるまで待機するフィールドを選択します。このアクティビティは、そのフィールドの場所を指定する文字列変数 (セレクター) を生成します。

## **プロパティ**

**共通**

* **エラー時に続行 (ブール値)** - ブール変数には、True または False の 2 つの可能な値があります。
  - True: アクティビティ内でエラーが発生した場合でも、プロセスの残りの部分が実行を継続できるようにします。
  - False: プロセスの実行の継続をブロックします。
* **タイムアウト MS (Int32)** - アクティビティの実行を待機する時間 (ミリ秒単位)。タイムアウトが経過すると、ページはそれ以上読み込まれなくなります。
デフォルト値: 30000 (ミリ秒)。
* **Delay After (Int32)** - アクティビティ実行後の遅延時間 (ミリ秒単位)。
例: 300
* **Delay Before (Int32)** - アクティビティが操作の実行を開始するまでの遅延時間 (ミリ秒単位)。
例: 300

**入力**

* **セレクター (文字列)** - 指定されたフィールドを識別する文字列。

**その他**

* **公開 (チェックボックス)** - 公開する場合にチェックを入れます。使用する前に、データ セキュリティ要件を必ず考慮してください。
* **表示名 (文字列)** - このアクティビティの名前。アクティビティの名前を編集して、コードをより適切に整理および構造化できます。

**オプション**

* **オートメーション ベース (ドロップダウン リスト)** - オートメーション ライブラリのバージョンを指定します。このオプションは、Process 引数と一緒に使用します。
* **From (UiElement)** - セレクター文字列で要素を検索する場合、ルートへの UiElement 変数 (入力されていない場合は、デスクトップのルートから検索します)。
* **ウィンドウを使用する (WindowApp)** - オープン ウィンドウまたはウィンドウ スコープからの出力変数を使用して、アクティビティのスコープを設定します (From と同様)。

**出力**

* **見つかった要素 (AutomationElement)** - 結果として得られる UI 要素。このフィールドは、UiElement 変数のみをサポートします。
